import { Inter, Ubuntu_Mono, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
export const ubuntuMono = Ubuntu_Mono({ subsets: ['greek'], weight: ['400', '700'], variable: '--font-ubuntu-mono' });
export const lusitana = Lusitana({ subsets: ['latin'], weight: ['400', '700'], variable: '--font-lusitana' });